---
layout: post
title:  "11.Neural_Network"
date:   2017-11-20 00:10:25 -0500
categories: Machine_Learning
published: false
---

Neural Networks!


PERCEPTRON! again

XOR is NOT linearly separable - 3 perceptron

perceptron is a threshold (step function)
0 아니면 어떤 값이다. 왜 0인가?

hard to tell from the output who contributed what.

twiddling little bit of weight would do nothing on the outcome

gradient descent 이티 손가락 터치 교육할때 ㅋㅋㅋ

gradient J has bound 0 < J << infinite

Rosenblatt
Minski and pepper


Summation of inputs (w_i * w_i)-> linear function -> differentiable

only ever gonna learn 'that hyperplace'

- perceptron
- linear
- sigmoid

horizontal - sum of inputs

differentiability is key

BACKPROPAGATION algo

gradient descent - batch(한번 하고 다시 또 다하고.. old-fashioned) / stochastic / mini-batch (subsets of the data)

update of weight OR update(잡음) of x

구글 무인차에 scattered 데이터를 던져주면..? 테러 - road냐 어린이냐




step function limit -> no effect in the result before get to a certain point!
solutino- > revmoce the step func

combining linear functions only gives me linear functions

multiple perceptron -> after working thru the math -> new single perceptron

3 nodes mixture -> better than 1 node


no guarantee for not having overfitting with 100 laeyrs
뉴럴 네트웤 -> 실수하기 쉽다. 카메라 픽셀 하나가 고장났나면?

ReLU -Rectified /Linear Univt
  below 0 -> 0

il

DESING CHOICES
1st gredient descent to try: ADAM

RULES of THUMB?
NO

using all hidden layer as a linear -> kinda pointless

start with tensor flow tutorial

share weights / feature map

specific number of feature maps / shared weights / patch size
=> no recognition of small capibara

down sampling

convolutional layer

specific number of feature maps / shared weights / patch size
=> no recognition of small capibara
-> design choice. first with small layer
residual network? skipped layer design(bypass / go thru)



multiple feature maps -> e.g. horizontal, vertical

learn from input -> hill climbing