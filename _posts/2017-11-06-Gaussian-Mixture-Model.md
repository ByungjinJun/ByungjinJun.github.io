### **Gaussian Mixture Models**

No direct estimation of real numbers
estimation! (simplify the problem)
픽 원옵 뎀 두 (가우시안 분포) 값을 비교해서


Once leanred, use the probabiitry dist. to
- make new examples
- classify data we havent seen before

height dist.
-> 200 marks (simplified)

파라미터 모델을 움직이면서 (파라메타를 약간 수정하면서)
캐피탈 쎄타 -> 모델 파라미터 (머신러닝)
 => anything with finite number of parameters

arbitrary shape with multiple gaussian!
그냥 임의의 펑션은 쉽지 않을 것이다 와이? parameter에 따라

log (y_1y_2) = log(y_1) + log(y_2)



머신러닝 포뮬라 -> 가우시안 분포 보이면?a
-> 걍 쓰면 된다.
진짜?

low dimension? -> plot it.
K(number of gaussian)


((((((ESP games -> image labeling)))))))))))

NO closed for msolution
varialbe need to learn -> num of w * 뮤 * 시그마 * K

Or

Expectation maximization (heal climbing)

감마 is a responsibility for a single data point